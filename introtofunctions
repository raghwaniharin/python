"""
this program is an intro to the use of functions
author Harin Harish
"""


def is_even(i):
    """
    :param i: number user inputs to check if it is even or not
    :return: returns statement saying whether number is even or not
    """
    if i % 2 == 0:
        print(str(i) + " is an even number")
    else:
        print(str(i) + " is not an even number")


x = int(input("enter a number that you want to check if it is even or not"))
is_even(x)
print(is_even.__doc__)
